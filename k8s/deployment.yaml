apiVersion: apps/v1
kind: Deployment
metadata:
  name: frontend-ventas
  labels:
    app: frontend-ventas
spec:
  replicas: 1
  selector:
    matchLabels:
      app: frontend-ventas
  template:
    metadata:
      labels:
        app: frontend-ventas
    spec:
      containers:
        - name: frontend-ventas
          image: gcr.io/construtem/frontend-ventas:latest
          ports:
            - containerPort: 3000
